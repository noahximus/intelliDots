# mac-extras: media handling, document conversion, and the communication
# apps that are wanted on a fully-appointed Mac but are not needed to make
# the machine usable.

brew "ffmpeg"      # Command-line audio/video conversion and processing toolkit.
brew "chromaprint" # Audio fingerprinting library (fpcalc) used by music taggers.
brew "poppler"     # PDF rendering and utilities (pdftoppm, pdftotext, etc.).
brew "librsvg"     # Converts and renders SVG files; used by document tooling.
brew "pandoc"      # Universal document converter for Markdown, HTML, PDF, and more.
brew "btop"        # Terminal system monitor for CPU, memory, disk, and processes.

cask "basictex"       # Smaller TeX distribution for LaTeX and PDF generation.
cask "iina"           # Modern macOS video player.
cask "discord"        # Voice, chat, and community messaging app.
cask "messenger"      # Facebook Messenger. Deliberately not in
                      # mac-essentials: it was commented out of the old
                      # Brewfile-essential, i.e. excluded by default.
cask "microsoft-edge" # Microsoft Edge web browser.
cask "slack"          # Team chat and messaging.
cask "zoom"           # Video meetings.
